server:
  port: 8761
eureka:
  client:
    register-with-eureka: false # it will stop registering eureka server itself with registry.
    fetch-registry: false    # it will not allow the same server to use it for any comm purpose.
